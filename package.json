{
  "name": "lyniq-media-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "minify": "npm run build",
    "dev": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.27.0"
  }
}
