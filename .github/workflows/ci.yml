name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Verify dist output (Wartungsmodus)
        run: |
          test -f dist/index.html
          test -f dist/wartung.html
          test -d dist/api
          test -d dist/assets
          grep -q "Website im Umbau" dist/index.html
